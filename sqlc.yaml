version: "2"
sql:
  - engine: "postgresql"
    queries: 
      - "./internal/adapters/storage/postgresql/sqlc/queries.sql"
    schema: "./internal/adapters/storage/postgresql/migrations"
    gen:
      go:
        package: "repo"
        out: "./internal/adapters/storage/postgresql/sqlc"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_interface: true # generates Querier interface for dependency injections